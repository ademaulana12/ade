#include <stdio.h>

void main() {
	//Deklarasi variabel lokal;
	int loop = 10,i;
	
	//Logika dasar pengulangan for;
	//JIka menampilkan tambahkan pointer pada saat mencetak;
	for(i=0; i<loop; i++) {
		printf("%i\n",i);
	}
}
